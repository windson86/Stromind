# Configure these as Netlify environment variables. Never commit real credentials.
STROMIND_SMTP_USER=bewerbung@stromind.de
STROMIND_SMTP_PASS=replace-in-netlify-only
STROMIND_APPLICATION_TO=bewerbung@stromind.de
