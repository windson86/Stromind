# STROMIND STATUS — MOVED

The canonical operational project status is maintained on the **`dev` branch** in `STROMIND_STATUS.md`.

Do not use this branch copy as the source of truth. For every resumed Stromind session:

1. Read `STROMIND_STATUS.md` from `dev`.
2. Verify current GitHub branch heads/divergence.
3. Work/test on `dev`.
4. Treat `main` as production and only merge there deliberately after testing.

This pointer exists only to prevent old chats or branch-specific work from treating `legal/jdoo-gdpr-ready` as the active workflow branch.
